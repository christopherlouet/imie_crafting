{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Leg creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('leg') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
